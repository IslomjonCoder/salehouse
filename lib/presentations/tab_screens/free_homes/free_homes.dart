import 'package:flutter/material.dart';

class FreeHomesScreen extends StatelessWidget {
  const FreeHomesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Free Homes'),
      ),
    );
  }
}
